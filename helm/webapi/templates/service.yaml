apiVersion: v1
kind: Service
metadata:
  name: {{ .Chart.Name }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http 
      port: 80
      targetPort: {{ .Values.service.targetPort }}
  selector:
    app: {{ .Chart.Name }}